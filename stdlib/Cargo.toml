[package]
name = "lkr-stdlib"
description = "Standard library for LKR (Query Check Language)"
version = "0.1.3"
edition = "2024"
authors = ["lollipopkit <a@lpkit.cn>"]
license = "Apache-2.0"

[dependencies]
lkr-core = { path = "../core" }
anyhow = { workspace = true }
chrono = { workspace = true }
tokio = { workspace = true, optional = true }

[features]
default = ["all"]
# Concurrency support
concurrency = ["tokio", "lkr-core/concurrency"]
# Keep a single umbrella feature for compatibility; all code is always built.
all = ["concurrency"]
